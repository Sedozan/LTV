comps = ["lifetime_premium","lifetime_loss","balance_amt","cat_loss_amt","claims_exp",
         "lifetime_exp","commission_exp_renew","commission_exp_new","cost_of_capital_20pct",
         "acquisition_exp","tax","investment_income"]
for c in comps:
    lvl = row[c] - row[c + "_v1_2"]          # true level difference
    dif = row.get(c + "_diff", np.nan)        # what the diff column stored
    if abs(lvl - dif) > 1e-6:
        print(f"{c:28s} level_diff={lvl:12.6f}  _diff={dif:12.6f}  Δ={lvl-dif:12.6f}")
