def score_cols(prefix):
    cols = [ ... ]  # the list as-is
    return cols

for tag, df, pfx in [("curr", df_curr, "e006scl"), ("prev", df_prev, "e005scl")]:
    want = score_cols(pfx)
    missing = [c for c in want if c not in df.columns]
    print(f"{tag}: {len(missing)} missing ->", missing)
